{
  "name": "jake-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Jake, uma central pessoal com IA, automacao local, memoria e financeiro.",
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint"
  },
  "workspaces": [
    "apps/web",
    "apps/desktop"
  ],
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
